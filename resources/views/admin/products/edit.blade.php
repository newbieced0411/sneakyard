<x-layouts.admin :title="'Edit '.$product->name"><div class="admin-page-heading"><div><h1>Edit product</h1><p>Update listing details, size inventory, and publishing status.</p></div><flux:button variant="ghost" icon="arrow-top-right-on-square" href="{{ route('products.show', $product) }}">View listing</flux:button></div><form class="admin-form" method="POST" action="{{ route('admin.products.update', $product) }}" enctype="multipart/form-data">@csrf @method('PUT') @include('admin.products.form')<div class="admin-actions"><flux:button variant="ghost" href="{{ route('admin.products.index') }}">Back</flux:button><flux:button variant="primary" type="submit">Save changes</flux:button></div></form><form method="POST" action="{{ route('admin.products.destroy', $product) }}" onsubmit="return confirm('Archive this product? It can no longer be purchased.')" style="margin-top:24px">@csrf @method('DELETE')<flux:button variant="danger" type="submit" icon="archive-box">Archive product</flux:button></form></x-layouts.admin>
