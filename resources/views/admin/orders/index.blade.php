<x-layouts.admin title="Orders">
    <div class="admin-page-heading"><div><h1>Orders</h1><p>Review, confirm, fulfill, and track every customer order.</p></div></div>
    <form class="admin-filters" method="GET"><input class="admin-input" type="search" name="search" value="{{ request('search') }}" placeholder="Order, customer, or email"><select class="admin-select" name="status"><option value="">All statuses</option>@foreach($statuses as $status)<option value="{{ $status->value }}" @selected(request('status') === $status->value)>{{ $status->label() }}</option>@endforeach</select><flux:button type="submit" icon="funnel">Filter</flux:button>@if(request()->hasAny(['search','status']))<flux:button variant="ghost" href="{{ route('admin.orders.index') }}">Clear</flux:button>@endif</form>
    <div class="admin-table-wrap"><table class="admin-table"><thead><tr><th>Order</th><th>Customer</th><th>Status</th><th>Payment</th><th>Items</th><th>Total</th><th>Placed</th></tr></thead><tbody>
        @forelse($orders as $order)<tr><td><a href="{{ route('admin.orders.show', $order) }}">{{ $order->order_number }}</a></td><td>{{ $order->customer_name }}<br><small>{{ $order->customer_email }}</small></td><td><span class="status-badge {{ $order->status->value }}">{{ $order->status->label() }}</span></td><td><span class="status-badge {{ $order->payment_status->value }}">{{ ucfirst($order->payment_status->value) }}</span></td><td>{{ $order->items_count }}</td><td>{{ $order->formatted_total }}</td><td>{{ $order->placed_at?->format('M j, Y g:i A') }}</td></tr>@empty<tr><td colspan="7">No orders match the current filters.</td></tr>@endforelse
    </tbody></table></div><div class="pagination-wrap">{{ $orders->links() }}</div>
</x-layouts.admin>
